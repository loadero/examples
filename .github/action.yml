name: "Loadero test"
description: "Runs Loadero test"
author: "Loadero"
runs:
  using: "node12"
  main: "javascript/loadero-action/dist/index.js ${{ secrets.ACCESS_TOKEN }}${{ secrets.PROJECT_ID }} ${{ secrets.TEST_ID }}"
